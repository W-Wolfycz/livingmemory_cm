# LivingMemoryCM 本地测试依赖。
#
# 本地测试不再导入真实 AstrBot core/backend，因此不再需要以下仅为让真实 core
# 可导入的依赖：audioop-lts、deprecated、sqlalchemy、sqlmodel、apscheduler、
# loguru、aiohttp、anthropic、google-genai、jsonschema、mcp、openai、pillow、
# psutil、pypdf、quart、certifi 等。AstrBot core 兼容性由 Windows 测试端
# reload/部署验收。
#
# 本文件同时包含测试运行真实需要的插件运行依赖（pytz/aiofiles/jieba 等），
# 使测试 venv 只安装本文件即可独立跑完整测试；与 requirements.txt 一起安装
# 也不冲突。

pytest>=9.0,<10
pytest-asyncio>=1.0,<2
pydantic>=2.12,<3
aiosqlite>=0.20.0
pyyaml>=6.0.2
pytz>=2021.3
aiofiles>=23.0.0
jieba>=0.42.1
numpy>=1.26,<3
